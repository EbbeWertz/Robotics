cmake_minimum_required(VERSION 3.8)
project(isfr_bot_mapping)

find_package(ament_cmake REQUIRED)

# Install robot description files
install(DIRECTORY config launch
        DESTINATION share/${PROJECT_NAME})

ament_package()