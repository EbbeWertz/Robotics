# Gemaakt met:
```bash
ros2 pkg create --build-type ament_cmake isfr_bot_bringup --dependencies rclcpp webots_ros2_driver
```

# Wat is dit?
Dit is basically de "main" package die alles launched.

## Folders
Eigen folders:
 - `./launch/`: Launch scripts voor verschillende setups

## Files
Deze package heeft geen speciale files